machine:
  registries:
    config: {}
      # Example for ghcr.io (this will be rendered by Terraform):
      #"ghcr.io":
      #  auth:
      #    username: ${ghcrUsername}
      #    password: ${ghcrPassword}
